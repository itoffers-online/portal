<div class="card">
    <div class="card-header" id="headingOne">
        <h4 class="mb-0">
            <button class="btn btn-link" data-toggle="collapse" data-target="#question-{{ number }}" aria-expanded="false" aria-controls="question-{{ one }}">
                {{ question }}
            </button>
        </h4>
    </div>

    <div id="question-{{ number }}" class="collapse" aria-labelledby="headingOne" data-parent="#accordion">
        <div class="card-body">
            <p>{{ answer }}</p>
        </div>
    </div>
</div>