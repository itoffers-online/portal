{% extends 'base.html.twig' %}

{% block body %}
<div class="album py-5 bg-light">
    <div class="row">
        {% if specializations.has('php') %}
            {% include 'offer/post/specialization.html.twig' with {specialization: specializations.get('php')} only %}
        {% else %}
            {% include 'offer/post/not_ready.html.twig' with {'name': 'PHP Developer'} only %}
        {% endif %}

        {% if specializations.has('javascript') %}
            {% include 'offer/post/specialization.html.twig' with {specialization: specializations.get('javascript')} only %}
        {% else %}
            {% include 'offer/post/not_ready.html.twig' with {'name': 'JS Developer'} only %}
        {% endif %}

        {% if specializations.has('devops') %}
            {% include 'offer/post/specialization.html.twig' with {specialization: specializations.get('devops')} only %}
        {% else %}
            {% include 'offer/post/not_ready.html.twig' with {'name': 'Devops Engineer'} only %}
        {% endif %}
    </div>
</div>
{% endblock %}